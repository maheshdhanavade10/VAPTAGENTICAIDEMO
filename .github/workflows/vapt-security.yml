name: CodeQL Analysis (C#)

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:

  # ---------------------------------------------------------
  # JOB 1: CodeQL Analysis (C#)
  # ---------------------------------------------------------
  codeql:
    name: CodeQL
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '8.0.x'

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: csharp

      - name: Restore dependencies
        run: dotnet restore

      - name: Build solution
        run: dotnet build --configuration Release --no-restore

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v3


  # ---------------------------------------------------------
  # JOB 2: SonarCloud Analysis (DISABLED)
  # ---------------------------------------------------------
  # sonar-analysis:
  #   name: SonarCloud Scan
  #   runs-on: windows-latest
  #   permissions:
  #     contents: read
  #     security-events: write
  #
  #   steps:
  #     - name: Checkout Code
  #       uses: actions/checkout@v4
  #       with:
  #         fetch-depth: 0
  #
  #     - name: Setup .NET
  #       uses: actions/setup-dotnet@v3
  #       with:
  #         dotnet-version: '8.0.x'
  #
  #     - name: Setup JDK 17
  #       uses: actions/setup-java@v3
  #       with:
  #         java-version: '17'
  #         distribution: 'temurin'
  #
  #     - name: Install Sonar Scanner
  #       run: |
  #         dotnet tool install --global dotnet-sonarscanner
  #         echo "$HOME/.dotnet/tools" >> $GITHUB_PATH
  #
  #     - name: SonarCloud Analysis
  #       env:
  #         GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  #         SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
  #         SONAR_PROJECT_KEY: ${{ secrets.SONAR_PROJECT_KEY }}
  #         SONAR_ORG_KEY: ${{ secrets.SONAR_ORG_KEY }}
  #         SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
  #       run: |
  #         dotnet-sonarscanner begin `
  #           /k:"$env:SONAR_PROJECT_KEY" `
  #           /o:"$env:SONAR_ORG_KEY" `
  #           /d:sonar.login="$env:SONAR_TOKEN" `
  #           /d:sonar.host.url="$env:SONAR_HOST_URL"
  #
  #         dotnet build
  #
  #         dotnet-sonarscanner end /d:sonar.login="$env:SONAR_TOKEN"


  # ---------------------------------------------------------
  # JOB 3: OWASP Dependency Check (SCA)
  # ---------------------------------------------------------
  # owasp-dependency-check:
  #   name: OWASP Dependency Check (SCA)
  #   runs-on: ubuntu-latest
  #   if: always()
  #
  #   steps:
  #     - name: Checkout repository
  #       uses: actions/checkout@v4
  #
  #     - name: OWASP Dependency Check
  #       uses: dependency-check/action@v3.0.0
  #       with:
  #         project: "VAPTAGENTICAIDEMO"
  #         path: "."
  #         format: "ALL"
  #         out: "dependency-check-report"
  #
  #     - name: Upload Dependency Check Report
  #       uses: actions/upload-artifact@v4
  #       with:
  #         name: owasp-dependency-check-report
  #         path: dependency-check-report


  # ---------------------------------------------------------
  # JOB 4: OWASP ZAP Baseline Scan (DAST)
  # ---------------------------------------------------------
  # owasp-zap:
  #   name: OWASP ZAP Baseline Scan (DAST)
  #   runs-on: ubuntu-latest
  #   if: always()
  #   env:
  #     APP_URL: http://localhost:5000
  #
  #   steps:
  #     - name: Checkout repository
  #       uses: actions/checkout@v4
  #
  #     - name: Build API Docker image for scan
  #       run: |
  #         docker build -t vapt-testing-demo.api:ci -f VaptTestingDemo.API/Dockerfile VaptTestingDemo.API
  #
  #     - name: Run API container for scan
  #       run: |
  #         docker run -d --rm --name vapt-testing-demo-ci -p 5000:80 vapt-testing-demo.api:ci
  #
  #     - name: Wait for API to be ready
  #       run: |
  #         for i in {1..30}; do
  #           if curl -sSf "$APP_URL/api/test/auth" >/dev/null; then
  #             echo "API is up"; break
  #           fi
  #           echo "Waiting for API... ($i)"; sleep 2
  #         done
  #
  #     - name: ZAP Baseline Scan
  #       uses: zaproxy/action-baseline@v0.10.0
  #       with:
  #         target: "${{ env.APP_URL }}"
  #         cmd_options: "-a"
  #
  #     - name: Stop API container
  #       if: always()
  #       run: docker stop vapt-testing-demo-ci || true
  #
  #     - name: Upload ZAP Report
  #       uses: actions/upload-artifact@v4
  #       with:
  #         name: zap-report
  #         path: zap-report.html


  # ---------------------------------------------------------
  # JOB 5: Auto-fix and PR Creation (DISABLED)
  # ---------------------------------------------------------
  # auto-fix-pr:
  #   name: Auto-fix and Create PR (SonarCloud + CodeQL)
  #   runs-on: ubuntu-latest
  #   needs: [owasp-dependency-check]
  #   if: always()
  #   permissions:
  #     contents: write
  #     pull-requests: write
  #
  #   steps:
  #     - name: Checkout repository
  #       uses: actions/checkout@v4
  #       with:
  #         persist-credentials: true
  #         fetch-depth: 0
  #
  #     - name: Setup .NET SDK
  #       uses: actions/setup-dotnet@v3
  #       with:
  #         dotnet-version: '8.0.x'
  #
  #     - name: Run .NET format auto-fixer
  #       run: |
  #         dotnet format --verify-no-changes || dotnet format
