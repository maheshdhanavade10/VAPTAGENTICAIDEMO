name: "CodeQL"

on:
  # push:
  #   branches: [ "main" ]
  # pull_request:
  #   branches: [ "main" ] # This triggers the Autofix on your PRs
  workflow_dispatch:

jobs:
  analyze:
    name: Analyze
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: write
      security-events: write
      pull-requests: write # Required to post the fix on the PR

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Setup .NET SDK
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'

    - name: Initialize CodeQL
      uses: github/codeql-action/init@v3
      with:
        languages: 'csharp'
        queries: security-extended,security-and-quality

    - name: Restore and Build
      run: |
        dotnet restore VaptTestingDemo.API/VaptTestingDemo.API.csproj
        dotnet build VaptTestingDemo.API/VaptTestingDemo.API.csproj --configuration Release

    - name: Perform CodeQL Analysis
      uses: github/codeql-action/analyze@v3
      with:
        category: "/language:csharp"